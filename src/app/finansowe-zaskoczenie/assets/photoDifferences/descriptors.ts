import { DifferenceDescriptor } from '@/components/photoDifferences/difference';

import banknoteDescriptor from './banknote/descriptor';

export const descriptors: DifferenceDescriptor[] = [banknoteDescriptor];
Object.freeze(descriptors);
